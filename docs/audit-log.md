# 監査ログ

## 2026-09-15 初期表示のfile:///対応

- 原因を実ブラウザで再現：正本・公開JSONには施設3件とR06→R08の比較情報が存在したが、Edgeのfile:///ではJSON fetchがCORS制限で失敗。施設欄0件・実行ボタン無効を確認した。修正前のHTTP経由のブラウザテストは成功した。
- `export_impact.py`で同一のエクスポート結果からJSONとブラウザ用JSを生成するよう修正。監査用正本は`data/impact-catalog.json`のまま。手入力・機械抽出の区別も維持した。正本JSON・公開JSONの再生成前後のSHA-256がそれぞれ一致し、内容変更がないことを確認。
- HTTP・fileとも通常のscriptタグでデータJS→操作JSを読み込む方式に統一。判定ロジック・集計値は変更せず、施設選択が1件以上の場合だけ実行ボタンを有効にした。
- HTML側に静的な起動案内・欄のプレースホルダーと外部スクリプトのエラー監視を追加。欠落・破損・データ形式不正を画面内に表示し、JavaScript無効時も案内が残る。file:///の実行エラーでファイル名が取得できない場合には原因の詳細を断定しない。
- Python 10件（既存・実マスター回帰を含む、skipなし）、Node 6件が成功。JS実行結果と公開JSONの完全一致を確認した。
- EdgeのHTTP localhost・file:///両経路で施設3件、比較改定、ボタン有効化、単独3区分／同時選択、8制度項目と根拠、対象外表示、解除を実操作で検証。1280px・390pxの画面を記録した。
- 両経路でデータJS欠落・操作JS欠落・両JSそれぞれの構文破損・データ形式不正の計10失敗ケースを確認。JavaScript無効時の案内も両経路で確認。画面記録は`work/startup-*.png`。
- commit、push、公開設定変更は行っていない。

## 2026-09-15 施設別の改定影響チェッカーMVP

### 既存設計と監査の再利用

- 作業フォルダにはv0.3のローダー、スキーマ、図鑑、監査文書、指定した両年度CSVが存在した。このフォルダを実装対象とした。ZIP自体の照合・再展開は行っていない。
- 既存`revision`・`code_item`・`code_kubun_map`・`code_facility_req`・`my_facility`を確認し、SQLテーブルは追加しなかった。制度注釈・明示コード対応は`data/impact-catalog.json`、機械抽出結果は`assets/impact-data.json`に分けた。
- 既存の改定差分、監査ログ、制約、データ出典を再利用した。新設予定124・廃止予定80の裏付けは既存監査記録を継承し、XLSXの再探索は行っていない。マスター仕様のp.26–27も既存記録を再利用した。

### 施設とコードの区別

- 実データで脳血管Ⅰと廃用Ⅰに施設基準枠732が現れることを確認。732だけで同じ選択肢にまとめず、`H001-I`・`H001-2-I`を別IDとして保持した。日本語名称と施設基準コードの完全な照合が済んだとの扱いにはしない。
- 運動器Ⅰの基準枠検索にはⅢのコードも現れる。Ⅰの明細は最初の等級表記が（１）のコードに限定して明示リストを確定した。括弧中の別の（１）に反応する候補抽出も修正した。
- 共通の旧加算と各疾患の新加算は制度項目の所属として保持する。コードの同一性・後継コードの自動推定は行わない。旧「リ減」と新「特定患者」は別の制度IDとし、直接の移行対応を作らなかった。
- 手入力のFact／Interpretationと機械抽出のFactをデータ・画面で区別。今回のエージェントによる確認を人の承認とみなさず、`human_review=null`とした。

### 一次資料とページ

- [個別改定項目](https://www.mhlw.go.jp/content/10808000/001655176.pdf)の既存監査対象を再照合。本文555–557（特定患者）、558–559（総合計画・旧減算）、560（リンパ浮腫）、561–563（早期・休日）を主張単位で参照する。本文561はPDF通し573なので、リンクと表示用ページを分けた。
- 施設だけでは条件を確定できない根拠として[正式通知・医科点数表（0730訂正後）](https://www.mhlw.go.jp/content/12400000/001732089.pdf)を確認。H001は本文499–500、H001-2は502–503、H002は504–506を各区分の関係に接続した。入院・患者区分等の差を一律の算定条件へまとめていない。
- 告示の[医科点数表](https://www.mhlw.go.jp/content/12400000/001686842.pdf)も参照先を確認したが、全項目の照合を完了したとは扱わない。細かな例外・全疑義解釈を推測で追加していない。
- 初期加算・急性期加算・データ提出加算はコード分割とマスター点数を収録。要件の年度間比較は未収録と明記した。

### 検証

- 指定CSVを既存ローダーで別の全件DBへ取り込み、R06 10,192件・R08 11,833件を再現。元の`work/med-code-map.db`を変更していない。
- 単位系：172→210、消滅75・新設113・同一コード点数変更0。H区分：190→234、消滅80・新設124・同一コード点数変更1。別々の集計として再検証した。
- Python 9テスト（既存3＋追加6）とNode 5テストが成功。私有マスターを使うテストもskipなしで実行。区分枝番号、H008の名称、C006の集計範囲、異なる年度・欠損・根拠不足の扱いを検証した。
- Edgeをheadlessで実起動し、図鑑からの移動、施設選択、8項目すべて条件付き、区分別コード、PDFページURL、対象外表示、選択解除、JSON読込失敗を検証。1280pxと390pxのスクリーンショットを確認し、横はみ出しとJavaScriptエラーがないことを確認した。
- 根拠の長い表示で比較内容が埋もれないよう、主張ごとに展開できる表示へ修正した。画像はGit管理外の`work/impact-*.png`に保存。
- GitHubへのpushと公開設定変更は行っていない。

## 2026-09-14 令和8年度版への対応

### 修正6：改定をまたいだ版の混在

- 旧状態：医科診療行為は令和6年度版の最終期（`s_20260501.csv`）、傷病名は2026年6月1日更新版を使用していた。2026年6月1日に令和8年度改定が施行されているため、診療行為だけが改定前の版だった。
- 入力：`s_20260911.csv`、11,833件、150列。全件の変更年月日が`20260601`以降、廃止年月日は全件`99999999`。
- 確認：列レイアウトは令和6年度版と同一で、ローダーの変更なしに取り込めた。単位系（項番8=28）は172件から210件へ増えた。
- 判断：新しい版へ差し替えるだけでは、改定で何が変わったかを引けない。`docs/limitations.md`に「同一コードの複数版を同時に保持できない」と記録していた制約を、ここで解消する方針とした。

### 修正7：複数改定の併存（スキーマ v0.3）

- 旧実装：`code_item`と`code_quantity_rule`の主キーが`(cs_id, code)`で、2つ目の改定を入れると衝突した。
- 修正：両テーブルへ`revision_id`を追加し、主キーを`(cs_id, code, revision_id)`とした。`code_kubun_map`と`code_facility_req`の外部キーも改定込みで参照するよう変更した。
- 修正：`load_master.py`へ`--append`を追加した。既存DBの複製へ書き込んでから差し替えるため、追加に失敗しても元のDBは残る。同じ改定IDが既にある場合は中止する。
- 追加：`sql/revision-diff.sql`（新設・消滅・点数改定・名称変更・区分番号ごとの増減・上限単位の変更・施設基準の要求変更）。名前付きパラメータを使うため、`queries.sql`とは分けた。
- 検証：合成した2版を1DBへ取り込み、新設・消滅・点数差が期待どおり抽出されることを`tests/test_smoke.py`で確認した。同じ改定IDの二重取込が拒否され、既存DBが変化しないことも併せて検証した。

### 修正8：差分の解釈の誤り（自己指摘）

- 誤り1：「廃止75件・新設113件」を集計範囲を添えずに記載した。これは項番8=28（単位系）の数字であり、H区分全体では廃止80件・新設124件である。両範囲はどちらも他方を含まない。
- 誤り2：「同一コードの点数変更0件」から「点数が変わっていない」と読んだ。制度上は早期リハビリテーション加算が25点から60点へ増点されているなど、点数変更は複数ある。マスターでは旧コード廃止＋新コード追加として表現されるため、同一コードの突合には現れない。
- 誤り3：旧「リ減」72件と新「特定患者」88件について、点数と件数が一致することを根拠に名称変更ではないかとした。両者はいずれも所定点数の90%であり、同じ基準値に同じ係数をかけた結果として点数が一致するにすぎない。一次資料では、旧「リ減」は目標設定等支援・管理料の未算定による減算、新「特定患者」は離床を伴わない他動的訓練に対する減算で、算定対象が異なる。令和8年度改定では目標設定等支援・管理料と当該減算規定そのものが廃止されている。
- 修正：`docs/revision-diff-R06-R08.md`を書き直し、集計範囲の明示、制度変更とコード差分の区別、撤回した仮説とその理由を記載した。`docs/limitations.md`へ「同一コードの突合だけでは制度上の点数変更を追えない」「点数の一致は対応関係の根拠にならない」を追加した。

### 修正9：区分番号の枝番号を落としていた

- 旧実装：区分番号を項番85（英字）＋項番92（数字）だけで復元し、項番93の枝番号を無視していた。
- 影響：`H001`（脳血管疾患等リハビリテーション料）と`H001-2`（廃用症候群リハビリテーション料）が同じ区分に潰れていた。令和8年度版の「H001 114件」は、実際には脳血管57件と廃用症候群57件の合計である。`H007`も障害児（者）3件とがん患者リハ1件の合計だった。
- 誤り：`KUBUN_NAME`で`H008`を「摂食機能療法」としていた。H008は集団コミュニケーション療法料であり、摂食機能療法はH004である。単位系に摂食機能療法は含まれていない。
- 修正：項番93を読み、`00`以外の有効な枝番号がある場合は`H001-2`の形で区分番号を復元するようにした。`H001-2`と`H007-2`の名称を追加し、`H008`の名称を訂正した。
- あわせて図鑑の本文で、単位系210件を「いずれも第7部リハビリテーションに属する」と記載していた点を訂正した。C006 在宅患者訪問リハビリテーション指導管理料は第2部在宅医療であり、第7部ではない。正しくはH区分208件とC006の2件である。
- この誤りは、区分番号を独立した識別子として正確に保持するという本リポジトリの設計目的に反するものだった。レセ電コードと区分番号を別軸に分けても、区分番号内の階層を欠落させれば、異なる区分が同一のものとして扱われる。

## 2026-09-08 公開版作成時

### 修正1：医科診療行為マスターの版

- 旧実装：`REVISION = ("R08", ...)`を固定。
- 入力：`s_20260501.csv`、10,192件。
- 一次資料：厚生労働省「ファイルダウンロード」では、10,192件の2026-05-01更新ファイルは令和6年度版（2026-05-31まで適用）。
- 修正：ローダーから固定値を除去し、改定ID・表示名・適用期間をCLIで必須入力にした。READMEの再現例は`R06`とした。

### 修正2：傷病名の日付列

- 旧実装：項番22を`changed_on`として取り込んでいた。
- 一次資料：令和8年度版マスターファイル仕様説明書「ファイルレイアウト」p.219-220では、項番22は収載年月日、23は変更年月日、24は廃止年月日。
- 修正：`listed_on`、`changed_on`、`abolished_on`の3列へ分離した。

### 修正3：移行先の外部キー

- 旧実装：移行先なしを許すため、`new_code`へ外部キーを張っていなかった。
- 判断：SQLiteの外部キーは`NULL`を許す。非NULLの移行先だけ参照整合性を検証できる。
- 修正：`new_code REFERENCES disease(disease_code)`を追加した。

### 修正4：検証失敗時のDB

- 旧実装：データをコミットした後に期待件数を検証し、失敗時もDBが残った。
- 修正：新規DBは一時ファイルへ構築し、取込と検証が完了した場合だけ完成名へ移す。既存DBへの傷病名追加はトランザクション内で行う。

### 修正5：図鑑のFact／Inference

- 旧表示：観測値から推測した運用理由を、事実と同じ文体で記載していた。
- 修正：観測・仕様・仮説のラベルを付け、逆引き、理論上の組合せ数、アウトカムの「空白」などの断定範囲を限定した。


## 9区分への拡張（2026-09-15）

3疾患とも、現時点の対応表は次のとおり。72対応を正本に明示登録し、未確認・対象外にはコード所属を登録しない。既存Ⅰの条件付き対応は維持する。

| 制度項目 | Ⅰ | Ⅱ | Ⅲ |
|---|---|---|---|
| 早期リハビリテーション加算 | 条件付き | 条件付き | 条件付き |
| 休日リハビリテーション加算 | 条件付き | 条件付き | 条件付き |
| 初期加算 | 条件付き | 要確認 | 要確認 |
| 急性期リハビリテーション加算 | 条件付き | 要確認 | 要確認 |
| データ提出加算 | 条件付き | 要確認 | 要確認 |
| 特定患者に対する疾患別リハ料 | 条件付き | 条件付き | 条件付き |
| 旧「リ減」廃止 | 条件付き | 条件付き | 条件付き |
| 総合計画評価料 | 条件付き | 条件付き | 対象外 |

追加確認した一次資料は[令和8年度医科診療報酬点数表（告示）](https://www.mhlw.go.jp/content/12400000/001686842.pdf)。PDF通しp.230–235のH001・H001-2・H002本体1〜3と各注、p.236–237のH003-2注1・2を使用。正本の`scope_*`に項目・疾患ごとのURL・注番号・物理ページを記録した。旧リ減の廃止は既存の個別改定項目p.558–559とR06の等級別明示コードを再利用。機械抽出だけで制度廃止を確定しない。

初期・急性期・データ提出加算について、本文に関連注があることと、追加施設要件が各等級に適用できることは分ける。Ⅱ・Ⅲの追加施設要件の告示・通知の照合は未完了（18対応）。人による最終確認も未実施であり、対応済みと表示しない。総合計画評価料のⅢ対象外は「その届出単独」の関係であり、他の届出を持つ施設全体の算定可否を否定しない。

正本は`data/impact-catalog.json`。`src/export_impact.py`で監査済みSQLiteから属性を付加し、JSONとブラウザJSを同時生成。SQL変更・外部API・認証は追加しない。Fact／Interpretation、手入力／機械抽出を維持する。

起動はフォルダ全体を保持して`impact.html`をダブルクリック（file:///）、またはルートで`py -m http.server 8000 --bind 127.0.0.1`を実行して`http://localhost:8000/impact.html`を開く。従来の読込失敗時の画面内案内も維持する。


### 拡張後の実行結果

- `python -m unittest discover -s tests -v`：11件成功（既存テストと実CSVの再取込を含む、スキップなし）。
- `node --test tests/impact.test.cjs`：8件成功。9識別子、72対応、単独・複数選択、未登録を要確認とする判定、生成JSとJSONの一致を検証。
- `.venv/Scripts/python tests/browser_impact.py`：Edge headlessでHTTP・file両方成功。9区分の個別／同時操作、7／8カードと対象外追加、根拠展開、ページ番号付きリンクのクリック、ボタン、リセットを確認。外部リンク先の応答だけはテスト用に置換し、外部PDFビューアの表示は検証していない。
- JS欠落・構文破損・データ不正の10ケースとJavaScript無効時の案内に成功。幅1280／390で表示確認、横はみ出しなし。`work/grades-file-mobile.png`等を目視確認。
- 単位系172→210、消滅75・新設113・同一コード点数変更0。H区分190→234、消滅80・新設124・同一コード点数変更1を維持。H001とH001-2、H008の名称、C006除外を回帰検証。
- 正本からブラウザJSON・JSを再生成済み。SQL・ロード処理・GitHub公開設定は変更せず、commit・pushは未実施。

## 2026-09-15 結果画面の表示整理

- 原因：`assets/impact.js`が正本の監査文と監査用語を通常の結果カードにも出し、主張・区分・コード単位で同じ最終確認文と根拠を繰り返していた。制度変更と対応・根拠の正本は`data/impact-catalog.json`、コード属性は`src/export_impact.py`で生成したJSON/JSに保持されている。
- 修正：結果カードに判定別の説明、年度比較、確認事項、主な根拠リンクを残し、監査情報を初期状態で閉じた詳細欄に移した。資料URLとPDFページ、根拠ID、主張の確度・入力経路、レビュー状態、マスター属性は詳細欄で確認できる。専門職による最終確認はカード内で集約して1回だけ表示する。空コードリストは作らない。
- 範囲：正本JSON、ブラウザ用生成データ、9施設基準・72対応、既存8項目、判定ロジック、コード対応と集計値は変更していない。新しい制度上の解釈や一次資料上の主張は追加していない。
- 検証：Python既存11件、Node既存8件が成功。Edgeでは`file:///`とlocalhostの両方で9区分の個別・同時選択、折りたたみの初期閉鎖、判定別説明、根拠リンク、コード未登録表示と読込失敗時の案内を確認した。ブラウザテストの初回は動的な`:checked`一覧から解除するテスト手順でタイムアウトしたため、選択IDを固定して解除するよう修正。再実行で両経路・失敗時の10ケースが成功。通常・監査詳細のカードを実寸で目視確認した。
